apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - namespace.yaml

namespace: whoami-prod

replicas:
  - name: whoami
    count: 2

patches:
  - target:
      kind: Ingress
      name: whoami
    patch: |-
      - op: add
        path: /spec/rules/0/host
        value: whoami.prod.k8s-demo.locl
